# ==============================================================================
# ntuple.sub - CMSSW15 TPS-Onia2MuMu ntuple node submit template
# ==============================================================================
# Variables injected by dag_generator.py:
#   campaign / job_id / analysis / max_events / cleanup / efficiency_ntuple / miniaod_input
#   local_output_base  (may be empty for t2_xrootd mode)
#   target_eos_base / custom_output_subpath / custom_ntuple_basename  (may be empty)
#   request_cpus / request_memory / request_disk
#   ntuple_bundle_path / ntuple_bundle_name / proxy_bundle_path / proxy_bundle_name
#   ntuple_wrapper_path / ntuple_wrapper_name
#   log_root
# ==============================================================================

Universe = vanilla

Executable = /bin/bash
transfer_executable = False
Arguments = "./$(ntuple_wrapper_name) $(ntuple_bundle_name) $(proxy_bundle_name) $(analysis) $(campaign) $(job_id) $(max_events) $(efficiency_ntuple) $(cleanup) $(miniaod_input) $(local_output_base) $(target_eos_base) $(custom_output_subpath) $(custom_ntuple_basename)"

Should_Transfer_Files = YES
WhenToTransferOutput = ON_EXIT

Transfer_Input_Files = $(ntuple_bundle_path), $(proxy_bundle_path), $(ntuple_wrapper_path)

transfer_output_files =

Output = $(log_root)/ntuple_$(campaign)_$(job_id)_$(Cluster)_$(Process).stdout
Error  = $(log_root)/ntuple_$(campaign)_$(job_id)_$(Cluster)_$(Process).stderr
Log    = $(log_root)/ntuple_$(campaign)_$(job_id)_$(Cluster).log

request_cpus = $(request_cpus)
request_memory = $(request_memory)
request_disk = $(request_disk)

Requirements = (TARGET.Machine =!= "nd-11.hepthu.com") && (TARGET.Arch == "X86_64") && (TARGET.OpSys == "LINUX") && (TARGET.Disk >= RequestDisk) && (TARGET.Memory >= RequestMemory) && (TARGET.Cpus >= RequestCpus) && (TARGET.HasFileTransfer)

+MaxRuntime = 604800

Environment = "HOME=/srv LOCAL_OUTPUT_BASE=$(local_output_base)"

OnExitHold = (ExitCode != 0)
OnExitHoldReason = "Ntuple chain failed"
OnExitHoldSubCode = 1
PeriodicRelease = (JobRunCount < 2) && (HoldReasonCode == 3)

Queue 1
